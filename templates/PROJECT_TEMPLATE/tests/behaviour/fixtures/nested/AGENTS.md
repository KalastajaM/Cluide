# Nested loading fixture

Read `../../../../AGENTS.md` for the root project policy. Do not execute a real task.
Final outputs belong in `../../../../Outputs/`; scratch belongs in `../../../../Working/`.
For source-only sessions, request missing sources rather than claiming ancestor files loaded.
